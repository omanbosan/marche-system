# 開発履歴

おまんぼさん受注管理システムの変更記録。新しいものが上。

各版の実物は `releases/index_vNN.html`・`releases/gas_vNN.gs` に残してある。
設計の背景・注意点・再発防止メモは [CLAUDE.md](CLAUDE.md) の各日付の節に詳しく書いてある。

- 本番フロントエンド: https://omanbosan.github.io/marche-system/
- 本番API（GAS）: デプロイID `AKfycbwQ8-M1...GF28Ulg`
- 案内ページ（GAS）: デプロイID `AKfycbyGx2qJ...`

---

## 2026-09-23

### v108 — 「◯分待ち」を完成時刻の横に大きく表示
- 見出しの小さな「+40分」をやめ、完成時刻の右隣に「**40**分待ち」と大きく表示（数字は時刻と同じ大きさ、30分超で橙・60分超で赤）
- 幅を確保するため、820px以下ではマスコット画像を外した。400px以下は数字を一回り小さく
- 360〜1400pxで「120分待ち」でも重ならないことを確認（320pxの旧iPhone SEのみ収まらない）

### v107 — 現在時刻と完成目安の時刻を色分けして表示
- 待ち目安カードを「完成目安」に変え、**いまの時刻＋待ち目安**の時刻（例 10:27＋40分→11:07）を時計と同じ数字の箱で表示
- 色分け：いま＝茶色の時計（コロン点滅）／完成目安＝緑のカード（コロンは点滅しない）。見出しに「いま」「完成目安」のタグ
- 待ち分数は「+40分」として見出しに小さく表示。30分超で橙、60分超で赤。待ちが無いときは「待ちなし」
- 完成目安は時計の分が変わるたびに再計算（`renderWaitEta()`）
- 重なり対策：440px以下は日付を省いて「いま」だけ、370px以下は数字を一回り小さく、621〜760pxはステータスを2段目へ

### v106 — ヘッダーのタイトルを「受注管理システム」に変更
- 「おまんぼさん」＋サブタイトル「売上管理システム」をやめ、「受注管理システム」のみに
- タイトルの箱を時計・待ち目安と同じ高さにそろえた。620px以下は「受注管理／システム」の2行（1行だと時計と重なるため）
- 2行にして幅が空いたので、v104で入れた「370px以下でマスコットを隠す」指定は外した

### v105 — ヘッダーのタイトル・時計・待ち目安の文字バランスを統一
- 3つの文字サイズを共通の変数（`--hs` 小見出し／`--hn` 数字／`--hl` タイトル）で管理し、画面幅ごとに変数だけ切り替えるようにした
- 待ち目安も時計と同じ「上に小見出し・下に数字の箱」の形にし、カードの枠・影・高さを時計とそろえた（「約」「分」は小さく）
- 待ちドットは小見出しの横に最大6個＋「+N」。スマホ幅では非表示（ステータスの「現地」件数で代用）

### v104 — 待ち時間バーを廃止し、待ち目安を時計の横へ移動
- ヘッダー下の「現地 待ち目安」バー（`.wbar`）を削除し、時計の右隣に小さなカード（`.hdr-wait`）として組み込んだ。待ちドットもカード内に表示
- 固定部の高さがバー1段ぶん（PCで約60px）減り、タブがヘッダーの直下に来る
- 440px以下はロゴを詰め、370px以下（小型Android）はマスコットを隠して1段目の重なりを防いだ

## 2026-09-21

この日は1セッションで v96 〜 v103 まで進めた。現場（マルシェ）での使用感に関する指摘を受けて直した内容が中心。

### v103 — スマホのヘッダーで時計を右上に移動
- 620px以下のヘッダーを「マスコット → ロゴ → 時計 → 同期ボタン」の1段目＋ステータスの2段目に組み替えた
- ヘッダーの高さが **118px → 102px** に縮み、本文に使える領域が増えた
- 実測：375/390/430px で時計が1段目の右端、横はみ出しなし

### v102 — スマホでの初回表示を高速化
「スマホで読み込みに時間がかかる」との報告を実測したところ、**原因はアプリ本体ではなく日本語Webフォント**だった。

| | サイズ |
|---|---|
| アプリ本体（gzip後） | 113KB |
| Google Fonts の CSS | **570KB**（`@font-face`が614個）← 読み終わるまで画面が出ない |
| フォント本体の合計 | 最大 7.8MB（分割366ファイル） |

- フォントCSSを `media="print"` + `onload` の**描画をせき止めない読み込み**に変更
- `fonts.googleapis.com` / `fonts.gstatic.com` に `preconnect` を追加
- Noto Sans JP の 500 を削除（直接指定は0件でほぼ未使用）
- フォールバックを端末標準の日本語ゴシックに変更（42箇所）

**効果（実測）**：First Contentful Paint **480ms → 276ms**、描画をせき止める通信 **149,255バイト → 0バイト**

### v101 — 「画面が固まりスクロールできなくなる」不具合を修正
原因は3つあり、いずれも**全画面を覆う要素が残って操作を受け付けなくなる**形だった。

1. **【主因】モーダルを背景タップで閉じるとスクロール禁止が解除されなかった**
   背景タップの経路が `closeOverlay` を通らず、`body` の `position:fixed` だけが残っていた。
   ロックを「`.overlay.on` が1つでもあるか」だけで決める方式に変え、`MutationObserver` でも監視するようにした
2. **`manualSync()` に try/finally が無く**、同期失敗時にローディングの膜とボタンの無効化が戻らなかった
3. **`fetch` にタイムアウトが無く**、電波が切れかけだと応答を待ち続けた
   → 60秒で打ち切る `gasFetch()` を追加。ローディングは8秒でタップして閉じられる案内を出し、70秒で自動解除

### v100 — 「弥生会計記帳」へ改称・専用タブへ分離
- 「クエスト」→ **「弥生会計記帳」** に改称
- 新タブ **「📒 記帳・ミッション」** を追加し、レベル・弥生会計記帳・今日のミッションを移動
- ホームは**ボタンのみ**（3つの要約ボタン、タップで専用タブへ）
- タブに**残件バッジ**（先月分の記帳＋今日のミッションの残数）
- 375〜1280px でボタンが1行に収まることを実測

### v99 — 入力モーダルが勝手に閉じる不具合の修正・ホーム画面の並び替え
- **入力欄から左へドラッグするとモーダルが消えて入力が失われる不具合を修正**。背景クリック判定が `click` の対象しか見ておらず、入力欄で押し始めて背景で離すと閉じていた。**押し始めも背景だったときだけ閉じる**ようにした
- 累計グラフの系列名を期間に合わせた（月次は「今月／◯月」、年次は「今年／◯年」）。「今期」表記が分かりにくいとの指摘
- 挨拶ブロックを削除し、クエストを最上段へ

### v98 — 2人で工程を分担する運用向けの修正
現場の運用（1人目が受付登録 → 2人目が2値化 → 1人目がデザイン、受付登録も2人が交互）を前提に検証し、3点を修正。

1. **自動同期を30秒 → 15秒に戻した**。相手の操作が見えるまでの時間が次の工程の待ち時間になるため。従来は20秒＋30秒の2本で実質12秒間隔だったので、v97の30秒1本では**待ち時間が倍以上に伸びていた**
2. **送信待ちの工程タップ・保留・入金確認を同期後に描き直す**ようにした。キューが詰まっている間に自動同期が走ると、押したはずの工程が戻って見える状態だった
3. **受付モーダルを開いたときに最新データを取り直す**ようにした。2人が交互に受付すると**同じ受付番号を提案してしまう**ため

### v97 — 読み書き高速化（楽観的UI＋送信キュー・在庫の一括処理・送信量削減）
「現地マルシェで2人で読み書きしているが動きが遅い」との相談への対応。

**調査で分かったこと**
- GASは通信1回あたり**最低約1.2秒**（放置後のコールドスタートは2.4〜3.5秒）
- 自動同期タイマーが**20秒と30秒の2本**動いていて、1台あたり毎分3〜5回 `getAll` を叩いていた
- 注文登録は `saveOrderFast` の応答を待ってから画面に反映しており、**3〜6秒画面が止まっていた**
- 在庫の引き落としが商品ごとに別通信＋**全体で1つのScriptLock**のため、2人同時登録で順番待ちになっていた
- `handleGetHistory` が O(n²)（履歴1件ごとに売上全件を走査）

**実施した内容**
1. 自動同期の整理（重複タイマー削除・多重実行の排除・非表示時はスキップ）
2. **送信キュー（アウトボックス）と楽観的UI**
   - `apiAsync()` の中身をキューへの登録に変更（呼び出し側は書き換え不要）
   - 受付・完了・削除・注文編集もキュー送信にし、画面はその場で更新
   - 1件ずつ順番に送るので「受付が未送信のまま完了を先に送る」事故が起きない
   - `localStorage` に保存し、ブラウザを閉じても次回起動時に続きから送る
   - 通信障害は何度でも再送。サーバー拒否のみ4回で停止しヘッダーに⚠️
3. **在庫の一括処理**（`adjustStockBatch`）。ロックとシート読み込みを1回にまとめた
4. **送信量の削減**。工程IDを「アイテムID-工程番号」に統一し `stepIds` の送信を廃止
   → 14点の注文で **2リクエスト6,164文字 → 1リクエスト3,355文字**
5. `handleSaveOrderFast` から history シート全件読み込みを削除

### v96 — ホーム画面を月次/年次対応に刷新
- 月次/年次の切替、先月・前年との比較（売上・注文件数・客単価・利益）
- グラフをライブラリ無しのSVGで実装（日別/月別売上、累計売上、曜日別、月別達成率、月別利益）。**1グラフ1軸**に統一し、数値を表でも確認できるようにした
- 年間/月間の売上・利益目標。「あと◯◯円」「1日あたりの必要額」「着地予想」「客単価なら何件」を表示
- 今日のミッションを売上UP・認知度UPにつながる内容へ変更（旧「3件完了」「在庫切れゼロ」は廃止）
- 記帳クエスト（売上金・経費・出展料・仕入れ/固定費）を追加
- 経費カテゴリに「出展料」を追加
- ヘッダーのロゴ（マスコット）を左上へ移動
- GASに `getDashSettings` / `saveDashSetting` を追加

---

## デプロイ記録

| 日付 | 対象 | 内容 |
|---|---|---|
| 2026-09-21 | GAS | **バージョン98** を作成し、2つのデプロイIDを両方 @98 に更新 |
| 2026-09-21 | フロント | v96 〜 v103 を GitHub Pages へ反映 |

GAS側は v97 の内容（`adjustStockBatch`・`serverStock`・`getDashData`・決定的ステップID など）で止まっている。
v98 以降はフロントエンドのみの変更のため、GASの再デプロイは不要。

---

## 検証のやり方

`window.fetch` をモックGASに差し替えたテストページを生成し、ヘッドレスChromeで実行する方式。
スクリプトは Claude Code のスクラップパッドに置いている（`mkwrite*.ps1` / `mkstock.ps1` / `mkhdr.ps1` / `perf.ps1`）。

**全23シーン**（最終的にすべてエラー0件）

| 分類 | シーン |
|---|---|
| ホーム画面 | 月次 / 年次 / 先月・目標なし / 操作一連 / サーバー保存済み / 旧GASフォールバック |
| 書き込み・送信キュー | 楽観的反映 / 圏外→復帰 / 送信順序 / サーバー拒否→手動再送 / 再読み込みをまたいだ再送と冪等性 / 送信サイズ / 削除と在庫復元 / 旧GASフォールバック / トークン切れ→再ログイン / 送信待ちのまま完了 |
| 2人運用 | 工程の受け渡し / 送信待ち中の同期 / 受付番号の重複回避 |
| UI | ドラッグでモーダルが閉じない / 画面の並び / 専用タブとバッジ / スクロール固着 / 通信タイムアウト |

GAS側は**旧版と新版に同じ入力を与えて出力を突き合わせる**方式で検証した。
- `handleGetHistory` の出力：4パターンすべて**完全一致**
- 在庫の最終値：7ケース（同一商品の複数点／タイプ別混在／現地・郵送混在／在庫連動／0での打ち止め／削除の戻し／存在しない商品）すべて**一致**

---

## 既知の制限・未着手の案

- **相手の操作が見えるまで最大15秒かかる**。GASはポーリングしかできないため、即時反映にはリアルタイムDB（Firebase等）への移行が必要
- **在庫の残数表示も同期間隔ぶん遅れる**。ただし引き落とし自体はサーバー側のロックで正しく処理されるので、数が壊れることはない
- **客単価には送料と割引が含まれる**（売上合計 ÷ 完了した注文件数）。商品代のみで見たい場合は要変更
- 未着手：完了済みの古い注文のアーカイブ（`getAll` の読み込み行数を一定に保つ）、Firebase等への移行、Webフォントの完全廃止（数百KB〜1MBの削減だが見た目が変わる）
